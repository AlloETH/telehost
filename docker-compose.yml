services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: telehost
      POSTGRES_PASSWORD: telehost
      POSTGRES_DB: telehost
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
